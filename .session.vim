let SessionLoad = 1
let s:so_save = &g:so | let s:siso_save = &g:siso | setg so=0 siso=0 | setl so=-1 siso=-1
let v:this_session=expand("<sfile>:p")
silent only
silent tabonly
cd ~/docs/work/projects/android/trip-logger/TripLogger
if expand('%') == '' && !&modified && line('$') <= 1 && getline(1) == ''
  let s:wipebuf = bufnr('%')
endif
let s:shortmess_save = &shortmess
set shortmess+=aoO
badd +22 app/src/main/java/com/triplogger/data/TripEntity.kt
badd +210 app/src/main/java/com/triplogger/ui/fragments/NewTripFragment.kt
badd +104 app/src/main/java/com/triplogger/ui/viewmodels/TripViewModel.kt
badd +123 app/src/main/res/layout/fragment_new_trip.xml
badd +61 app/src/main/res/layout/item_trip.xml
badd +45 app/src/main/java/com/triplogger/ui/adapters/TripAdapter.kt
badd +0 app/src/main/java/com/triplogger/utils/ExcelExporter.kt
argglobal
%argdel
edit app/src/main/java/com/triplogger/utils/ExcelExporter.kt
wincmd t
let s:save_winminheight = &winminheight
let s:save_winminwidth = &winminwidth
set winminheight=0
set winheight=1
set winminwidth=0
set winwidth=1
argglobal
balt app/src/main/java/com/triplogger/ui/adapters/TripAdapter.kt
setlocal foldmethod=indent
setlocal foldexpr=0
setlocal foldmarker={{{,}}}
setlocal foldignore=#
setlocal foldlevel=0
setlocal foldminlines=1
setlocal foldnestmax=20
setlocal foldenable
let s:l = 47 - ((46 * winheight(0) + 18) / 37)
if s:l < 1 | let s:l = 1 | endif
keepjumps exe s:l
normal! zt
keepjumps 47
normal! 0
tabnext 1
if exists('s:wipebuf') && len(win_findbuf(s:wipebuf)) == 0 && getbufvar(s:wipebuf, '&buftype') isnot# 'terminal'
  silent exe 'bwipe ' . s:wipebuf
endif
unlet! s:wipebuf
set winheight=1 winwidth=20
let &shortmess = s:shortmess_save
let &winminheight = s:save_winminheight
let &winminwidth = s:save_winminwidth
let s:sx = expand("<sfile>:p:r")."x.vim"
if filereadable(s:sx)
  exe "source " . fnameescape(s:sx)
endif
let &g:so = s:so_save | let &g:siso = s:siso_save
set hlsearch
nohlsearch
doautoall SessionLoadPost
unlet SessionLoad
" vim: set ft=vim :
